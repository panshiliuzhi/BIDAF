import tensorflow as tf

# def highway_networks(x, size, num_layers=2, scope="highway", reuse=None):
#     with tf.variable_scope(scope, reuse=reuse):
#         for i in range(num_layers):
#             w_t = tf.get_variable(name="gated%d"%i, shape=[])

